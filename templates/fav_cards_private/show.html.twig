{% extends 'base.html.twig' %}

{% block title %}FavCardsPrivate{% endblock %}

{% block body %}
    <h1>FavCardsPrivate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fav_cards_private.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ fav_cards_private.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fav_cards_private.description }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ fav_cards_private.link }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ fav_cards_private.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ fav_cards_private.createdAt ? fav_cards_private.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ fav_cards_private.updatedAt ? fav_cards_private.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fav_cards_private_index') }}">back to list</a>

    <a href="{{ path('app_fav_cards_private_edit', {'id': fav_cards_private.id}) }}">edit</a>

    {{ include('fav_cards_private/_delete_form.html.twig') }}
{% endblock %}
